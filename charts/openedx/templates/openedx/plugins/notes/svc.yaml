{{- if and .Values.openedx.enabled .Values.openedx.notes.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.openedx.notes.service.name | default "notes" }}
  annotations:
    {{- include "openedx.annotations" . | nindent 4 }}
  labels:
    {{- include "openedx.componentLabels" (dict "component" "notes" "root" .) | nindent 4 }}
spec:
  type: ClusterIP
  ports:
  - port: {{ .Values.openedx.notes.service.port | default 8000 }}
    protocol: TCP
    targetPort: {{ .Values.openedx.notes.application.port | default 8000 }}
  selector:
    {{- include "openedx.componentSelectorLabels" (dict "component" "notes" "root" .) | nindent 4 }}
status:
  loadBalancer: {}
{{- end -}}
