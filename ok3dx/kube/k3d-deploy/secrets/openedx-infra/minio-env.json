{
  "apiVersion": "v1",
  "kind": "Secret",
  "metadata": {
    "name": "minio-env",
    "annotations": {
      "ok3dx/pwgen": "config.env|export MINIO_ROOT_PASSWORD=(.+)|ascii_secret|24"
    }
  },
  "type": "Opaque",
  "stringData": {
    "config.env": "export MINIO_ROOT_USER=admin\nexport MINIO_ROOT_PASSWORD=the_minio_password"
  }
}
